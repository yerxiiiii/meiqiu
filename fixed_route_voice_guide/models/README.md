# Models (not in git)

Download locally under this directory:

- `sherpa-onnx-kws-zipformer-wenetspeech-3.3M-2024-01-01/` — KWS
- `vosk-model-cn-0.22/` — optional ASR (large)

Or place a `kws-model.tar.bz2` and extract as needed.
